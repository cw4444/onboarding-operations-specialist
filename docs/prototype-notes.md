# Prototype Notes

This folder contains a minimal TypeScript-style workflow scaffold for the demo.

It is intentionally small and easy to read:

- `models/` defines the onboarding case shape
- `engine/` contains validation, routing, and assistant-like helpers
- `demo/` contains sample data and a runnable example flow

The goal is to show the system design clearly, not to ship a full HR platform.
